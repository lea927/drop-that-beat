- if @tracks
  - @tracks.each do |track|
    %div
      %p
        %strong
          = track.name
        = 'by'
        = track.artist
      %audio{ controls: true }
        %source{ src: track.preview_url, type: 'audio/mp4' }
- else
  %p No tracks found
