%h1 
  = @room.name.upcase
  = 'ROOM'
%h3 STEP 1
%h3 Search a Song
= render 'tracks/search_tracks'
%h3 STEP 2
%h3 Create a Playlist
- if @room.tracks.any?
  %table 
    %thead 
      %tr
        %th Title
        %th Artist
        %th Action
    %tbody 
      - @room.tracks.each do |track|
        %tr 
          %td
            = track.name
          %td 
            = track.artist
          %td Remove
%h3 STEP 3
%h3 Set Rounds
= form_with model: @room, local: false do |f|
  = render partial: 'devise/shared/error_messages', locals: { resource: @room }
  = f.label :rounds
  = f.number_field :rounds, min: 1, step: 1, required: true
  %br
  = f.submit 'Finish setup'